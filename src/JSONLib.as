package
{
	import json.JParser;
	import ab.fl.utils.json.JSON;
	import flash.display.Sprite;

	/**
	 * Document class used to make the SWC library
	 * with Flash CS5.
	 * 
	 * @author Omar Gonzalez
	 */
	public class JSONLib extends Sprite
	{
		public function JSONLib()
		{
			ab.fl.utils.json.JSON;
			json.JParser;
		}
	}
}
